diff -ruN srt068-release/android/app/src/main/AndroidManifest.xml srt068-final/android/app/src/main/AndroidManifest.xml
--- srt068-release/android/app/src/main/AndroidManifest.xml	2026-08-01 15:24:20.000000000 +0000
+++ srt068-final/android/app/src/main/AndroidManifest.xml	2026-08-01 15:32:52.580630950 +0000
@@ -3,6 +3,7 @@
     <uses-permission android:name="android.permission.INTERNET" />
     <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
     <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
+    <uses-permission android:name="android.permission.RECORD_AUDIO" />
     <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
     <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
     <uses-permission android:name="android.permission.WAKE_LOCK" />
diff -ruN srt068-release/android/app/src/main/java/com/paul/smarttvremote/MainActivity.java srt068-final/android/app/src/main/java/com/paul/smarttvremote/MainActivity.java
--- srt068-release/android/app/src/main/java/com/paul/smarttvremote/MainActivity.java	2026-08-01 15:24:20.000000000 +0000
+++ srt068-final/android/app/src/main/java/com/paul/smarttvremote/MainActivity.java	2026-08-01 15:32:52.580630950 +0000
@@ -1,7 +1,9 @@
 package com.paul.smarttvremote;
 
+import android.Manifest;
 import android.app.Activity;
 import android.content.Intent;
+import android.content.pm.PackageManager;
 import android.graphics.Color;
 import android.os.Bundle;
 import android.speech.RecognizerIntent;
@@ -17,6 +19,7 @@
 
 public final class MainActivity extends Activity {
     private static final int VOICE_REQUEST = 4106;
+    private static final int AUDIO_PERMISSION_REQUEST = 4107;
 
     private WebView webView;
     private String pendingVoiceLanguage = "uk-UA";
@@ -62,10 +65,27 @@
         @JavascriptInterface
         public void start(String language) {
             pendingVoiceLanguage = language == null || language.isEmpty() ? "uk-UA" : language;
-            // RecognizerIntent is handled by the phone's system speech app.
-            // It owns microphone access, so requiring a second WebView/app
-            // RECORD_AUDIO permission only causes false "not allowed" errors.
+            // Preserve the existing Samsung path exactly: its WebView/native
+            // fallback still asks for the app microphone permission.
+            runOnUiThread(() -> {
+                if (android.os.Build.VERSION.SDK_INT >= 23
+                        && checkSelfPermission(Manifest.permission.RECORD_AUDIO) != PackageManager.PERMISSION_GRANTED) {
+                    requestPermissions(new String[]{Manifest.permission.RECORD_AUDIO}, AUDIO_PERMISSION_REQUEST);
+                    return;
+                }
+                launchVoiceRecognition();
+            });
+        }
+
+        @JavascriptInterface
+        public void startSystem(String language) {
+            pendingVoiceLanguage = language == null || language.isEmpty() ? "uk-UA" : language;
+            // Thomson / Google TV uses the phone's system recognizer directly.
+            // The recognizer application owns microphone access, avoiding the
+            // WebView SpeechRecognition "not-allowed" failure.
             runOnUiThread(MainActivity.this::launchVoiceRecognition);
         }
     }
@@ -82,6 +102,19 @@
         }
     }
 
+    @Override
+    public void onRequestPermissionsResult(int requestCode, String[] permissions, int[] grantResults) {
+        super.onRequestPermissionsResult(requestCode, permissions, grantResults);
+        if (requestCode == AUDIO_PERMISSION_REQUEST) {
+            if (grantResults.length > 0 && grantResults[0] == PackageManager.PERMISSION_GRANTED) {
+                launchVoiceRecognition();
+            } else {
+                sendVoiceError("Дозвіл на мікрофон не надано.");
+            }
+        }
+    }
+
     @Override
     protected void onActivityResult(int requestCode, int resultCode, Intent data) {
         super.onActivityResult(requestCode, resultCode, data);
diff -ruN srt068-release/remoteapp/web/index.html srt068-final/remoteapp/web/index.html
--- srt068-release/remoteapp/web/index.html	2026-08-01 15:24:20.000000000 +0000
+++ srt068-final/remoteapp/web/index.html	2026-08-01 15:32:52.580630950 +0000
@@ -1062,6 +1062,7 @@
 
       function speechConstructor() { return window.SpeechRecognition || window.webkitSpeechRecognition || null; }
       function hasNativeVoice() { return !!(window.NativeVoice && typeof window.NativeVoice.start === 'function'); }
+      function hasNativeSystemVoice() { return !!(window.NativeVoice && typeof window.NativeVoice.startSystem === 'function'); }
       function setVoiceListening(active) {
         listening = active;
         elements.voice.classList.toggle('danger', active);
@@ -1074,14 +1075,15 @@
       function startRecognition() {
         if (listening) return stopRecognition();
 
-        // Android WebView exposes a SpeechRecognition object on some phones but
-        // rejects microphone access with "not-allowed". The native Android
-        // RecognizerIntent is the reliable path, so prefer it whenever present.
-        if (hasNativeVoice()) {
+        // Android WebView exposes SpeechRecognition on some phones but rejects
+        // microphone access with "not-allowed". Use the native system recognizer
+        // only for Thomson / Google TV. Samsung keeps its previous voice path.
+        if (state.provider === 'google' && hasNativeSystemVoice()) {
           setVoiceListening(true);
           elements.voiceStatus.textContent = 'Відкриваю системне голосове введення…';
           try {
-            window.NativeVoice.start(elements.speechLanguage.value);
+            window.NativeVoice.startSystem(elements.speechLanguage.value);
           } catch (error) {
             setVoiceListening(false);
             elements.voiceStatus.textContent = `Помилка: ${error.message || 'не вдалося відкрити голосове введення'}`;
@@ -1091,6 +1093,17 @@
 
         const Recognition = speechConstructor();
         if (!Recognition) {
+          if (hasNativeVoice()) {
+            setVoiceListening(true);
+            elements.voiceStatus.textContent = 'Відкриваю системне голосове введення…';
+            try {
+              window.NativeVoice.start(elements.speechLanguage.value);
+            } catch (error) {
+              setVoiceListening(false);
+              elements.voiceStatus.textContent = `Помилка: ${error.message || 'не вдалося відкрити голосове введення'}`;
+            }
+            return;
+          }
           elements.voiceStatus.textContent = 'Голосове розпізнавання недоступне на цьому пристрої.';
           return;
         }
